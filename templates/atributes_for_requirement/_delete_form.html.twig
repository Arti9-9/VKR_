<form method="post" action="{{ path('app_atributes_for_requirement_delete', {'id': atributes_for_requirement.id,   'requirement': requirement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ atributes_for_requirement.id) }}">
    <button class="btn">Delete</button>
</form>
