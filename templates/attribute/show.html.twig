{% extends 'base.html.twig' %}

{% block title %}Attribute{% endblock %}

{% block body %}
    {{ include('component/navbar.html.twig') }}
    <h1>Attribute</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attribute.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ attribute.Name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ attribute.Value }}</td>
            </tr>
            <tr>
                <th>UnitMeasurements</th>
                <td>{{ attribute.UnitMeasurements }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_attribute_index') }}">back to list</a>

    <a href="{{ path('app_attribute_edit', {'id': attribute.id}) }}">edit</a>

    {{ include('attribute/_delete_form.html.twig') }}
{% endblock %}
