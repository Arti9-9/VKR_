{% extends 'base.html.twig' %}

{% block title %}Edit Auditorium{% endblock %}

{% block body %}
    <h1>Edit Auditorium</h1>

    {{ include('auditorium/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_auditorium_index') }}">back to list</a>

    {{ include('auditorium/_delete_form.html.twig') }}
{% endblock %}
