{% if is_granted('ROLE_ADMIN') %}
    <nav class="navbar navbar-expand-lg bg-dark bg-gradient">
        <a class="navbar-brand me-md-auto ps-5 d-flex" href="{{ path('app_users') }}">
            <img src="" width="50 " height="50" class="d-inline-block align-top" alt="">
            <p class="my-auto ">MTO</p>
        </a>
        <a class="btn  btn-outline-primary text mx-3" href="{{ path('app_auditorium_index') }}"
           role="button">Загрузить расписание</a>
        <a class="btn  btn-outline-primary text mx-3" href="{{ path('app_auditorium_index') }}"
           role="button">Аудитории</a>
        <a class="btn  btn-outline-primary text mx-3" href="{{ path('app_equipment_index') }}"
           role="button">Оснащенность</a>
        <a class="btn  btn-outline-primary text mx-3" href="{{ path('app_direction_index') }}"
           role="button">Направления</a>
        <a class="btn  btn-outline-primary text mx-3" href="{{ path('app_users') }}" role="button">Пользователи</a>
        <a class="btn  btn-outline-primary text mx-3" href="{{ path('app_users') }}" role="button">Шаблон справки</a>
        <a class='btn btn-outline-primary text mx-3' href="{{ path('app_logout') }} ">Выход</a>
    </nav>
{% else %}
    <nav class="navbar navbar-expand-lg bg-dark bg-gradient">
        <a class="navbar-brand me-md-auto ps-5 d-flex" href="{{ path('app_users') }}">
            <img src="" width="50 " height="50" class="d-inline-block align-top" alt="">
            <p class="my-auto ">MTO</p>
        </a>
        <a class="btn  btn-outline-primary text mx-3" href="{{ path('app_curriculum_index') }}"
           role="button">Учебные планы</a>
        <a class="btn  btn-outline-primary text mx-3" href="{{ path('app_direction_user_index', {'user' : app.user}) }}"
           role="button">Направления</a>
        <a class='btn btn-outline-primary text mx-3' href="{{ path('app_logout') }} ">Выход</a>
    </nav>
{% endif %}