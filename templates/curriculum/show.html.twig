{% extends 'base.html.twig' %}

{% block title %}Направление{% endblock %}

{% block body %}
    {{ include('component/navbar.html.twig') }}
    <h1>Направлениие: {{ curriculum.direction }}</h1>
    <h1>{{ curriculum.name }}</h1>
    <h5>Дисциплины:</h5>
    <ul>
    {% for disciplin in curriculum.disciplines %}

        <li><p>{{ disciplin.name }}</p></li>
    {% endfor %}
    </ul>
    <a href="{{ path('app_curriculum_index') }}">back to list</a>

    <a href="{{ path('app_curriculum_edit', {'id': curriculum.id}) }}">edit</a>

    {{ include('curriculum/_delete_form.html.twig') }}
{% endblock %}
