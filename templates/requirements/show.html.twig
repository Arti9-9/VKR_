{% extends 'base.html.twig' %}

{% block title %}Requirements{% endblock %}

{% block body %}
    <h1>Requirements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ requirement.id }}</td>
            </tr>
            <tr>
                <th>NameEquipment</th>
                <td>{{ requirement.nameEquipment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_requirements_index') }}">back to list</a>

    <a href="{{ path('app_requirements_edit', {'id': requirement.id}) }}">edit</a>

    {{ include('requirements/_delete_form.html.twig') }}
{% endblock %}
